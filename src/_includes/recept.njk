---
layout: 'base-recept.njk'
---
{% set bereidingswijze = bereidingswijze or "" %}

<h2>{{ title }} {% for tag in tags -%}{% if tag !== 'post' %}<span>({{ tag }} / {{personen}} personen)</span>{% endif %}{%- endfor %}</h2>

<div id="bereidingswijze">
  <p class="bold">Bereidingswijze</p>
  {{ bereidingswijze | nl2p | safe }}
</div>
<div id="ingredienten">
  <p class="bold">Ingredi&euml;nten</p>
  {{ content | safe }}
</div>
<figure>
  <img src="{{ image }}" alt="︱foto nog niet ingesteld">
  <figcaption>
    {{ imageAlt }}
  </figcaption>
</figure>
